/*
 * uds_reset.c
 *
 *  Created on: 31 mar. 2026
 *      Author: HP
 */

#include "flash_if.h"

void UDS_Reset(uint8_t *req, uint16_t len)
{
    uint8_t resp[2] = {0x51, req[1]};
    ISO_TP_Send(resp,2);

    HAL_Delay(100);

    Boot_JumpToApplication(0x08020000);
}
